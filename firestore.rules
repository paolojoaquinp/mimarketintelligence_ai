rules_version = '2';

// Restrictive default — deny all access until authenticated rules are defined.
// Why: Security-first approach per design.md (Firebase App Check + QA requirements).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
