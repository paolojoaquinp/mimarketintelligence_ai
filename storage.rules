rules_version = '2';

// Restrictive default — deny all access until authenticated upload rules are defined.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
